import { createGlobalStyle } from "styled-components";


export const lightModeTheme = {
    headerBackgound: '#000000',
    headerColor: '#ffffff'
}

export const darkModeTheme = {
    headerBackgound: '#000000',
    headerColor: '#ffffff'
}